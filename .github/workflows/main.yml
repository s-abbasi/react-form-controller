name: MAIN

on:
  push:
    branches: [main]
    
jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repo
        uses: actions/checkout@v2
      - name: Update README.md version
        run:  -exec sed -i "s/VERSION/hoy/" README.md
      
