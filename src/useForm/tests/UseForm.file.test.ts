import { Form } from '../UseForm.types';

const formModel: Form = {
    img: { initialValue: '' },
};

describe('UseForm - typeof file', () => {
    test.todo('should set "form.img.value" to a binary string');
});
